{
  "buildCommand": "npm run build",
  "outputDirectory": "aifrontend/dist",
  "installCommand": "npm install --prefix aifrontend",
  "devCommand": "npm run dev --prefix aifrontend",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
